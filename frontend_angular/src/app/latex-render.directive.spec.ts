import { LatexRenderDirective } from './latex-render.directive';

describe('LatexRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new LatexRenderDirective();
    expect(directive).toBeTruthy();
  });
});
